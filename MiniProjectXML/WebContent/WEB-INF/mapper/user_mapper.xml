<?xml version='1.0' encoding='UTF-8'?>
<!DOCTYPE mapper PUBLIC "_//mybatis.org/DTD Mapper 3.0//EN"
						"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="user">
	<!-- 아이디 중복검사(유효성검사) -->
 	<select id="checkUserIdExist" parameterType="java.lang.String" resultType="java.lang.String">
 		<![CDATA[
	 		select user_name
			from user_table
			where user_id = #{user_id}
		]]>
 	</select>
 	
 	
 	<!-- 회원가입(사용자 정보 저장) -->
 	<insert id="addUserInfo" parameterType="kr.co.hyewon.beans.UserBean">
 		<![CDATA[
	 		insert into user_table (user_idx, user_name, user_id, user_pw) 
	 	    values (user_seq.nextval, #{user_name}, #{user_id}, #{user_pw})
		]]>
 	</insert>
</mapper>			

